CREATE TABLE caretaker_task (
	taskID varchar(10) not null,
	caretakerID varchar(10) not null,
	PRIMARY KEY (taskID, caretakerID)
);

ALTER TABLE caretaker_task
	ADD CONSTRAINT FK_caretakertask_caretakerID
    	FOREIGN KEY (caretakerID)
    	REFERENCES caretaker(caretakerID);

ALTER TABLE caretaker_task
	ADD CONSTRAINT FK_caretakertask_taskID
	FOREIGN KEY (taskID)
	REFERENCES task(taskID);